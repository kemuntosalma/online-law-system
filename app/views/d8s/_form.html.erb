<%= form_with(model: d8, local: true) do |form| %>
  <% if d8.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(d8.errors.count, "error") %> prohibited this d8 from being saved:</h2>

      <ul>
      <% d8.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section_number %>
    <%= form.number_field :section_number %>
  </div>

  <div class="field">
    <%= form.label :crime %>
    <%= form.text_area :crime %>
  </div>

  <div class="field">
    <%= form.label :law %>
    <%= form.text_area :law %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
